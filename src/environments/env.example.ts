/**
 * Full the content below with the needed data values
 * Remove the ".example" from the filename
 */

import { EnvironmentProps } from './env-types';

export const env: EnvironmentProps = {
  production: false,
};
